name: receipt_reader_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.3
  path: ^1.8.1
  camera: ^0.8.1+5
  google_ml_kit: ^0.6.0
  http: ^0.13.3
  image_picker: ^0.8.4+4

flutter:
  uses-material-design: true