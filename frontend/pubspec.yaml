name: receipt_reader_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.8.1+5
  google_ml_kit: ^0.6.0
  http: ^0.13.3

flutter:
  uses-material-design: true